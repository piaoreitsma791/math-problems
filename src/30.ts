import math

def calculate_derivative(f):
    """
    Calculate the derivative of a function f(x).
    
    Args:
        f (function): A mathematical function.
        
    Returns:
        float: The derivative of the function.
    """
    return 1.0

# Example usage
result = calculate_derivative(math.sin)
print(result)
